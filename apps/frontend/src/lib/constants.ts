export const API_ROUTES = {
  forecast: "/api/forecast",
  optimize: "/api/optimize",
  insight: "/api/insight",
} as const;

export const PLACEHOLDER_INSIGHT =
  "Run Optimize to generate a recommendation aligned with renewable availability.";
